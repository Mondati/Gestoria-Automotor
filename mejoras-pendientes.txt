MEJORAS DE DISEÑO PENDIENTES — Gestoría del Automotor
======================================================
Última actualización: sesión 4 (2026-05-03)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IMPLEMENTADO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[✓] Trust — mosaico asimétrico
    Grid 3 columnas: primera card ocupa 2 filas (dark bg / ink), última card
    ocupa 2 columnas. Crea un bento grid en lugar de 4 columnas iguales.

[✓] Contact — fondo oscuro + Footer unificado
    Sección 08 pasa a background: var(--ink). Footer eliminado como componente
    separado; su contenido (nav links, teléfonos, dirección, copyright) se
    integra dentro de la misma sección oscura como <footer> semántico.

[✓] Coverage — números dramáticos + count-up
    Fondo cambia a var(--ink). Números full-bleed a clamp(56px, 9.5vw, 160px).
    Animación count-up con IntersectionObserver y easing cúbico. Números
    alternados en var(--paper) y var(--gold).

[✓] Animaciones clip-path en Hero
    meta-bar y hero-actions usan reveal-clip (wipe left-to-right, cubic-bezier
    spring). SOFT axis de Fraunces anima de 0 → 30 durante el word-reveal del
    título. useReveal.js actualizado para observar .reveal-clip.

[✓] Cursor personalizado
    Componente Cursor.jsx con punto (5px, instantáneo) + anillo (30px, lerp 0.1).
    mix-blend-mode: difference con white → se invierte automáticamente en fondos
    claros y oscuros. Hover sobre a/button: punto desaparece, anillo crece a 48px.
    Solo activo en @media (pointer: fine).

[✓] Burgundy + contraste entre secciones
    Services: hover pasa de var(--ink) a var(--burgundy), num en var(--gold).
    Services: patrón diagonal (-45deg, cada 48px, opacidad 0.09).
    Advisory: advisory-quote-mark y advisory-quote-rule en var(--burgundy).
    Advisory: eyebrow-num 05 en var(--burgundy) (única sección con este color).
    Global: textura SVG body::before de opacity 0.05 → 0.09.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IMPLEMENTADO (sesión 5)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[✓] Mobile drawer desde la derecha
    .side-drawer usa transform: translateX(100%). Nav-burger con margin-right.

[✓] Hero meta-bar — opacity: 0.6

[✓] Services — toc-num: font-size: 18px; opacity: 0.30

[✓] Hero plaque mobile — position: static en 900px, 52px, grid-column: 1/-1,
    hero-plaque-meta text-align: left en mobile.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BUGS CORREGIDOS (sesión 5)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[✓] contact.css — Footer sin padding: .closing-footer-inner no existía en JSX.
    Padding y max-width movidos directamente a .closing-footer-nav.
    CSS muerto eliminado: .closing-footer-inner, .closing-footer-contact.

[✓] trust.css — Selector CSS inválido: .trust-cell reveal-d1 h3
    (faltaba el punto — no aplicaba a nada). Eliminado.

[✓] services.css — Clase muerta: .toc-item:hover .toc-dots
    (.toc-dots no existe en Services.jsx). Eliminada.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PENDIENTE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

— Sin items pendientes —
